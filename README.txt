Simple POS v2

New: daily transaction log, previous-day reports, print/PDF, share, editable products, top items, average order spend, all-time analytics.

Update existing GitHub Pages: replace index.html and sw.js, commit to main, wait for deployment, then open the POS URL on the iPad while online. Do not clear Safari website data because that can erase locally stored orders. Export a backup before major changes.
